function fullForm(id) {
  var idValue = document.getElementById("productId");
  idValue.value = id;

  var imageValue = document.getElementById("image");
  var image = document
    .getElementById(id)
    .querySelector(".imageProduct").innerHTML;
  imageValue.value = image;

  var nameValue = document.getElementById("image");
  var name = document
    .getElementById(id)
    .querySelector(".imageProduct").innerHTML;
  nameValue.value = name;

  var categoryValue = document.getElementById("image");
  var image = document
    .getElementById(id)
    .querySelector(".imageProduct").innerHTML;
  imageValue.value = image;

  var imageValue = document.getElementById("image");
  var image = document
    .getElementById(id)
    .querySelector(".imageProduct").innerHTML;
  imageValue.value = image;
}
